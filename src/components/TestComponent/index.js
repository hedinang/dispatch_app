import React, { Component } from 'react';
import {inject, observer} from "mobx-react";

export default class TestComponent extends Component {
  render() {
    return <div>This is TestComponent</div>
  }
}